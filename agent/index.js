console.log('Agent starting...');
// In the future, this will set up a WebSocket server 
// to listen for commands from the manager.
setInterval(() => {
    console.log('Agent is running...');
}, 10000);
